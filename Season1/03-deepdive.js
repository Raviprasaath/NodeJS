/*
Basically when we wrote a code first it will do parsing

then Lexical analysis (Tokenization) - it will break the code into small pieces and these pieces are called tokens

then syntax analysis (Parsing) - here tokens are converted into Abstract Syntax Tree (AST) and this AST is used to generate machine code

then it will go to Interpreter
there are 2 types of JS engine
1.Interpreter - line by line execute 
    fast initialization but slow execution
2.Complied - first it will compile convert into machine code and it will execute
    initially heavy but fast execution

Javascript is combination of both interpreter and compiler - Just in time compiler (JIT) - it will compile the code which is used frequently and it will interpret the code which is not used frequently


---lots of details are there but not taking as of now, it is available in video-SE1-08



will take example as Namesta dev.com

here frontend and backend are running in same machine/server
Database in diff server
Video and images in diff server







*/